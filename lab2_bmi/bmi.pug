DOCTYPE 
html(lang="en")

head 
    link(rel="stylesheet" href="bmi.css")
    link(rel="stylesheet" href="https://fonts.googleapis.com/css?family=Noto+Sans")
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    meta(http-equiv="X-UA-Compatible" content="ie=edge")
    title BMI Checker

body   
    div.header
        div.wrapper
        form(method="post")
            h1 BMI calculator
            p insert your age
            input.age(type="text" name="age"  value=strAge ? strAge:null )
            p insert weight in kg
            input.weight(type="text" name="weight"  value= strWeight ? strWeight:null )
            br
            p inser heihght in cm 
            input.height(type="text" name="height" value= strHeight? strHeight:null )
            br
            button.calc(action="/") check 
            p.result
            p= strBMI

        //- if(one.length==0){ 
        //-     p= one    
        //- }else{ 
        //-     p please enter values before submitting 
        //- } 
    
